pipeline {
    agent any

    stages {
        stage('Go Deps') {
            steps {
                sh 'go --version'
            }
        }
        stage('Ginkgo Tests') {
                    steps {
                        echo 'Hello from Jenkins World'
                    }
                }
    }
}
